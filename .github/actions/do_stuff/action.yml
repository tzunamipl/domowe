name: 'Wykonaj Akcję'

runs:
  using: "composite"
  steps:
    - name: Wydrukuj zmienne
      shell: bash
      run: |
        echo ${SEKRET}
        echo ${ZMIENNA}
        echo ${ZMIENNA_2}
        sleep 60
    - name: Sprawdz użytkownika
      shell: bash
      run: |
        whoami
