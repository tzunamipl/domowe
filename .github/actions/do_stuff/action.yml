name: 'Wykonaj Akcję'

runs:
  using: "composite"
  steps:
    - name: Wydrukuj zmienne
      shell: bash
      run: |
        echo ${SEKRET}
        echo ${ZMIENNA}
        echo ${ZMIENNA_2}
    - name: Sprawdz użytkownika
      shell: bash
      run: |
        whoami
